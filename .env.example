# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Set this before storing any real client credentials. Changing it later makes
# everything already encrypted permanently unreadable.
ENCRYPTION_KEY=
